[tool.poetry]
name = "western-ghats-monitor"
version = "0.1.0"

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "gunicorn backend.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --timeout 120 --access-logfile - --error-logfile -"
